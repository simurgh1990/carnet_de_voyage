import 'package:flutter/material.dart';

class WalletPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Carnet de Voyage - Wallet'),
      ),
      body: Center(
        child: Text('Contenu du Wallet de voyage!'),
      ),
    );
  }
}